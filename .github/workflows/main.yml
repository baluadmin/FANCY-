name: Bavesh-Fancy-Stationery-APK
on:
  workflow_dispatch:
  push:
    paths:
      - "Bavesh Fancy & Stationery Android/Bavesh Fancy & Stationery Android/**"

jobs:
  build:
    name: Build Android APK
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: Bavesh Fancy & Stationery Android/Bavesh Fancy & Stationery Android

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android SDK
        run: |
          sdkmanager "platform-tools"
          sdkmanager "platforms;android-35"
          sdkmanager "build-tools;35.0.0"

      - name: Install Gradle
        run: |
          wget -q https://services.gradle.org/distributions/gradle-8.9-bin.zip
          unzip -q gradle-8.9-bin.zip -d "$HOME/gradle"
          echo "$HOME/gradle/gradle-8.9/bin" >> "$GITHUB_PATH"

      - name: Check Gradle
        run: gradle --version

      - name: Build Debug APK
        run: gradle assembleDebug --no-daemon

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name:Bavesh Fancy & Stationery APK
          path: Bavesh Fancy & Stationery Android/Bavesh Fancy & Stationery Android/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
